[package]
name = "twilic-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde_json = "1"
twilic-bridge = { path = "../twilic-bridge" }
wasm-bindgen = "0.2"
getrandom = { version = "0.3", features = ["wasm_js"] }
